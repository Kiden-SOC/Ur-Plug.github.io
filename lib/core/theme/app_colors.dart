import 'package:flutter/material.dart';

class AppColors {
  static const Color brandPrimary = Color(0xFF005F73);      // Deep Ocean Teal
  static const Color brandSecondary = Color(0xFF0A9396);    // Rich Turquoise       
  static const Color screenBackground = Color(0xFFE0F2F1);  // Turquoise Ice Canvas
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF333333);
  static const Color textMuted = Color(0xFF999999);
  static const Color success = Color(0xFF1B4D3E);           // Deep Mint / Teal Green (high contrast for text/icons)
  static const Color textDark = Color(0xFF0A2229); 
  static const Color warning = Color(0xFFEE9B00); // Warm Amber / Sunset (high contrast against teal)
  static const Color accentRedOrange = Color(0xFFCA6702); // Rich Coral Amber / Red-Orange accent
}