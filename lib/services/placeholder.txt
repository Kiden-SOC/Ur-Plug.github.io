backend end work
ASSIGNEES: Jessie, Eleanor, Patience (Backend)

JESSIE'S CORE TASKS (Auth & Storage):
- Initialize Firebase Core in the flutter app.
.
- Write 'auth_service.dart' to handle sending OTPs and verifying user sessions.
- Build an Image Utility service using 'image_picker' and 'flutter_image_compress' to shrink photos before uploading them to Firebase Storage.

ELEANOR'S CORE TASKS (Product Queries & Caching):
- Write 'product_service.dart' to handle uploading new listings to Firestore.
- Write compound Firestore queries to filter product streams by category, price, and location simultaneously.
- Implement 'cached_network_image' and local caching (Hive/SharedPrefs) to save user data bundles on image reloads.

PATIENCE'S CORE TASKS (Real-Time Chat & System Triggers):
- Write 'chat_service.dart' to establish real-time Firestore message streams between buyer and seller UIDs.
- Implement 'url_launcher' logic so the "Call Plug" button opens the phone's native dialer with the seller's number.
- Configure Firebase Cloud Messaging (FCM) to send push notifications when a new chat message arrives.
